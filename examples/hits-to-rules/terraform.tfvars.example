api_token = "your-api-token"
api_host  = "https://us1.api.wallarm.com"

# Map of request_id → config JSON.
# Empty string "" = all rule types, request mode.
# Specify mode and/or rule_types per request_id as JSON.
request_ids = {
  "4666dee205d69757fd45cf94d2f0d7eb" = ""
  # "another_request_id" = "{\"mode\":\"attack\"}"
  # "filtered_request"   = "{\"rule_types\":[\"disable_stamp\"]}"
}
