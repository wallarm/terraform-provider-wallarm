1.0.0

**`data_source_wallarm_hits.go`**
- Fetches hits by `request_id`, validates consistency across hits
- Builds `action` conditions using `defaultResourceRuleActionSchema` (TypeSet) matching exact provider conventions — `value`/`point` fields per type, stable `SetId`

**`rules_disable_stamp/main.tf`**
- Seeds YAML config files from hits data on first apply using `fileexists()` round-trip instead of `ignore_changes` — eliminates stale-seed problem
- Reads files at plan time via `file()` — no `data "local_file"` / `depends_on` — eliminates perpetual `(known after apply)`
- `local.file_configs` falls back to `var.points` on first apply via `jsondecode(jsonencode(...))` for type normalization
- `stamp_rules` driven from `local.file_configs` so file edits are fully authoritative after seeding

**`templates/point_config.yaml.tftpl`**
- Human-readable, editable YAML — stamps, point, action all modifiable
- No metadata noise, clear comments explaining edit behavior