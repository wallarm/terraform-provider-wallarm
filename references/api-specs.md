# API Specifications

## Resources

- `wallarm_api_spec` — API specification upload + scope. URL-only (`file_remote_url`); file-upload mode tracked in `.claude/plans/roadmap.md` (gitignored).
- `wallarm_api_spec_policy` — per-violation block/monitor/ignore policy attached to an uploaded spec.

## Manual testing reference

`.claude/manual-testing/api_spec.md` (gitignored) — `wallarm_api_spec` + `wallarm_api_spec_policy` (17 scenarios, 10-15 min smoke pass at the bottom).
